# 全局参数（所有用例共享的基线参数；用例输入页的「用例参数 JSON」会覆盖同名项）
# 用例文本中不写 ${param.xxx}，参数统一在此维护；接口请求体字段在接口文档中声明 ${param.xxx} 引用
# 登录凭据：勿在此写明文。用环境变量 TEST_ADMIN_USER / TEST_ADMIN_PASSWORD 注入（executor.login 优先读环境变量）
# admin_user: admin
# admin_password: admin123

# 测试环境（BASE_URL 也可由 --base-url 或环境变量 BASE_URL 注入）
base_url: http://127.0.0.1:8080

# 常用测试资源（按实际环境填写）
# classroom_name: test_classroom
# strategy_name: test_strategy
# student_image_name: WIN10_VDI
# teacherIp: 10.10.10.10
