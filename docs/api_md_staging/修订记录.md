# RCC-Space 接口文档修订记录

> 记录 api_md_staging/ 接口文档的全部修订（生成后各轮检查/修复/增强）。
> 更新日期：2026-08

## 一、出参完整性与正确性

| 修订 | 范围 | 说明 |
|---|---|---|
| 出参合并行展开（字段名含 `/`） | 24 文档 | `vdiDesktopId/vdiDesktopIp/...` 拆为独立行 |
| 出参编造字段清除（`code`/`retCode`/`resultCode`） | 28 文档 | SK 框架无 code，改为标准五件套（status/message/msgKey/msgArgArr/content） |
| 出参字段补全（对照源码 DTO） | 68 候选文档 | seat_list 2→55、cloudDesktop_list 2→150、desktop_list 3→40、classroom_list 2→50、agreement_template 1→38 等 |
| vdi detail/getInfo 出参补齐 | 2 文档 | 8→32、1→32 字段（SpaceDeskStrategyGroupVDI 20 自有 + Abstract 11 继承 + id/name） |
| tci detail/getInfo 出参补齐 | 2 文档 | →20 字段（SpaceDeskStrategyGroupTCI） |
| 分页字段 `items`→`itemArr` | 4 处 | 框架真实字段（PageQueryResponse.getItemArr()/getTotal()，非猜测 items） |

## 二、断言可执行化

| 修订 | 范围 | 说明 |
|---|---|---|
| 成功/失败断言补 JSONPath | 149 文档 | `$.status=="SUCCESS"` + `$.msgKey` + content 字段断言 |
| "日志 key 当断言"清除 | 12 文档 | 断言改为 HTTP 响应级（status+msgKey），非服务端审计日志 |
| 异步接口断言补轮询终态 | 55 文档 | `轮询 content.taskId 至终态 batchTaskItemStatus∈[...]` |
| 纯操作接口（无 body）断言修正 | 6 文档 | halo_setCheck/closeTerminal/collectLog/dataPermission 等标"纯操作接口" |
| front-matter/正文断言不一致修复 | 多文档 | teacher_start 等 front-matter assertions 对齐正文（status==ERROR+msgKey） |

## 三、环境清理

| 修订 | 范围 | 说明 |
|---|---|---|
| cleanup internal:///状态机回滚 → HTTP 接口 | 21 文档 | 改为对应 delete 接口或标注"无对应 HTTP 清理接口" |
| 创建接口补 cleanup | 4 文档 | classroom/seat/strategy/networkWhitelist create |

## 四、参数驱动（方案 A）

| 修订 | 范围 | 说明 |
|---|---|---|
| setup 查询步骤补按名称过滤（searchKeyword/matchArr/exactMatchArr） | 171 文档 | 参数引用 `${param.xxx}`（名称→ID） |
| 查询规则表补全 | 17 查询接口 | desktop/seat/whitelist/cluster/space/publish 等（字段名读源码 DTO 核实） |
| 主请求体必填字段参数化 | 14 创建接口（78/78） | `${param.*}`/固定枚举/config_generator 标注 |
| 镜像分配接口补 cluster/storagePool/network setup | 4 文档 | teacher_create/student_create × rcc/spacetci（ID 绑定 0 悬空） |
| login 步骤统一"框架内置" | 211 文档 | 移除 token extract（$.content.token 待验证），引擎统一处理（SETUP_PARAM_SPEC §11） |

## 五、幂等标记

| 修订 | 范围 | 说明 |
|---|---|---|
| 创建类 setup 加 `idempotent: recreate` + `delete_api` | 189 步骤 | 存在同名先删再建；delete_api/delete_param 全配对（0 缺失） |

## 六、错误码

| 修订 | 范围 | 说明 |
|---|---|---|
| `62110019` 死码修正为 `62110012` | 1 文档（tci_create） | 源码 validDiskSize 实际抛 DISK_STRATEGY_EMPTY；62110019 全工程定义未抛 |
| 错误码对照表引用补充 | 27 文档 | 引用 code_map_all.md |

## 七、接口级深度修正

| 修订 | 范围 | 说明 |
|---|---|---|
| space_strategy_tci_create 全量修正 | 1 文档 | 断言/清理/失败返回/约束 4 问题（含参考文档 8 条约束 + 响应结构样例） |
| front-matter 分隔符粘连修复 | 17 文档 | `---# POST` → `---\n\n#`（YAML 语法错误，宽容解析会漏检） |
| 产出断言补 taskId | 1 文档 | space_strategy_tci_create 轮询 taskId 断言 |
| params 清单扩展 | 1 文档 | create_vdi 补 cpu/memory/systemSize |

## 八、辅助交付物

| 文件 | 内容 |
|---|---|
| README.md | 接口文档总览/索引（224 接口） |
| SETUP_PARAM_SPEC.md | 参数引用规范（${param.*}/${prev.*}/${context.*}）+ 幂等三值（true/false/recreate）+ 登录约定（§11）+ 用例模板（§12） |
| code_map_all.md | 全量码表（89 错误码 + 500 错误 key + 57 成功 key + 95 状态枚举 + 213 日志 key） |
| error_code_map_tci_strategy.md | TCI 策略错误码对照（24 个含触发条件 + 校验顺序） |
| 外部接口清单.md | 5 个外部接口（loginAdmin 内置/admin.list/file:///集群直连）处理方式 |
| 用例参数_VDI教师机镜像变更策略.md | 用例参数集 + 接口映射 + 断言示例 |

## 九、工具脚本（_audit/）

| 脚本 | 用途 |
|---|---|
| transform_setup_all.py | 全量 setup 参数化（幂等保护，已改步骤跳过） |
| param_main_request.py | 主请求体参数化 |
| add_idempotent_params.py | idempotent 标记批量加（含正则大小写修正） |
| preview_setup.py / verify_setup.py / dump_setup.py | setup 预览/验证/导出 |
| check_yaml_dup.py / check_endpoints.py / check_required_consistency.py | YAML 重复 key/端点/一致性检查 |

## 当前状态

```
接口文档: 224 个（YAML 0 失败、粘连 0、断言可执行 0、internal:// 0）
辅助文档: 6 个
工具: _audit/ 12 个脚本
参数引用: 172/224（74%）
创建接口必填字段: 78/78 全有取值方式
idempotent recreate: 189 步骤（全带 delete_api）
login 内置: 211 文档
```
